豆包的参数：
thinking取值范围：enabled， disabled，auto默认值{"type": enabled"}
reasoning取值范围：minimal，low，medium，high默认值 {"effort": "medium"}
caching取值范围：enabled， disabled默认值 {"type": "disabled"}
temperature取值范围： [0, 2]默认值：不知道，不填就行
top_p取值范围： [0, 1]默认值 0.7

千问的参数：
enable_thinking默认值：false
temperature默认值： 0.7
top_p默认值：0.8
presence_penalty默认值：0
repetition_penalty默认值：1.05
top_k默认值：1